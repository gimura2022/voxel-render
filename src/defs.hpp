#ifndef _defs_hpp
#define _defs_hpp

#include <string>
#include <string_view>

using char_t = char;

using str_t      = std::basic_string<char_t>;
using str_view_t = std::basic_string_view<char_t>;

#endif
